apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}
spec:
  ports:
  - port: {{ .Values.service.port }}
    protocol: TCP
    targetPort: {{ .Values.app.port }}
  selector:
    app: {{ .Values.app.name }}
  type: {{ .Values.service.type }}